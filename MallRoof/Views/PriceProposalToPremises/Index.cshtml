@model IEnumerable<MallRoof.Models.PriceProposalToPremise>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Premise.Number)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProposingUser.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Price)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Comments)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Premise.Number)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProposingUser.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Price)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Comments)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PriceProposalToPremiseId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PriceProposalToPremiseId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PriceProposalToPremiseId })
        </td>
    </tr>
}

</table>
